name = "voucher-registration-powermall"
compatibility_date = "2026-08-20"
pages_build_output_dir = "."

# Note: D1 (DB) and R2 (BUCKET) bindings are connected via
# Cloudflare Pages Dashboard -> Settings -> Functions -> D1 / R2 Bindings
